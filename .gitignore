node_modules
dist
.DS_Store

# --- secrets: never commit anything holding keys or passwords ---
# Covers .env.local, .env.cloud.bak, .env.production and any other variant.
# .env.example is the only one that ships — it has placeholders, not keys.
.env
.env.*
!.env.example

# the offline server's database password
server/config.json
